{
  "name": "adonis-mongo-rules-validator",
  "version": "3.0.1",
  "description": "Extended validator rules for mongo",
  "main": "index.js",
  "author": "Charlie",
  "keywords": [
    "mongoose",
    "adonis",
    "adonisjs",
    "mongodb",
    "validator",
    "rules"
  ],
  "license": "MIT",
  "peerDependencies": {
    "@adonisjs/fold": "^4.0.5"
  },
  "scripts": {
    "lint": "standard",
    "pretest": "npm run lint",
    "posttest": "npm run coverage",
    "test:local": "FORCE_COLOR=true node bin/index.js --local",
    "test": "nyc npm run test:local",
    "test:win": "set FORCE_COLOR=true && node bin/index.js --win",
    "coverage": "nyc report --reporter=text-lcov | coveralls"
  },
  "devDependencies": {
    "coveralls": "^3.0.0",
    "japa": "^1.0.6",
    "japa-cli": "^1.0.1",
    "nyc": "^11.4.1",
    "semver": "^5.5.0",
    "standard": "^11.0.0",
    "@adonisjs/fold": "^4.0.5"
  },
  "standard": {
    "global": [
      "use"
    ]
  }
}
